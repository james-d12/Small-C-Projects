#include <stdio.h>
#include <stdlib.h>

#include "../src/cmath.h"

int main()
{
    return 0;
}

/*
int main()
{
    Vec2 vec2 = vec2_new(10.5, 16);

    float mag = vec2_magnitude(vec2);
    
    vec2_pow(vec2,2);
    printf("mag: %f\n", mag);

    Mat2 mat2f = mat2_new(1,2,3,4);
    Mat2 mat2s = mat2_new(5,6,7,8);

    mat2f = mat2_scale(mat2f,2);

    mat2_print(mat2f);

    Mat2 res = mat2_mult(mat2f,mat2s);

    mat2_print(res);

    return 0;
} 
*/  